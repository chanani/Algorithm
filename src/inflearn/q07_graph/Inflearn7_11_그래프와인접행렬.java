package inflearn.q07_graph;

public class Inflearn7_11_그래프와인접행렬 {

    // 1. 무방향 그래프
    // 정점과 간선이 주어졌을 때 2차원 배열에 넣어서 사용
    // g[a][b] = 1; 로 넣었을 때 g[b][a] = 1; 도 똑같이 값을 넣어줘야함

    // 2. 방향그래프
    // 동일하게 2차원 배열에 넣어서 사용
    // g[a][b] = 1;로 넣어 사용할 수 있음

    // 3. 가중치 그래프
    // 방향 그래프에서 간선에 가중치 값까지 있는 그래프
    // g[a][b] = c; 로 사용할 수 있음

}
